-- Class definitions for the deep hierarchy test.
-- These classes are globals so they're accessible from the methods file.
RPElement = RPDefine("RPElement")
RPContainer = RPDefine("RPContainer", RPElement)
RPBaseFrame = RPDefine("RPBaseFrame", RPContainer)
