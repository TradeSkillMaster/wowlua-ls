---@meta _
-- LE_* constants used by popular addons but not referenced by Blizzard's
-- own FrameXML (so they are not discovered by the automatic FrameXML scan).
-- Add new entries here when running `check` against real addon projects
-- reveals missing LE_* globals.

LE_GAME_ERR_AUCTION_BID_OWN = 3
LE_ITEM_BIND_ON_ACQUIRE = 1
LE_ITEM_BIND_QUEST = 4
